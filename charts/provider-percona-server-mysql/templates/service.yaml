apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-percona-server-mysql.fullname" . }}
  labels:
    {{- include "provider-percona-server-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-percona-server-mysql.selectorLabels" . | nindent 4 }}

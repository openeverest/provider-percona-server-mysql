# Component type version catalog.
# Add new versions here when operator releases are available.
# Mark exactly one version per type as `default: true`.

componentTypes:
    mysql:
        versions:
            - default: true
              image: example/mysql:1.0.0
              version: 1.0.0
    orchestrator:
        versions:
            - default: true
              image: percona/percona-orchestrator:3.2.6-22
              version: 3.2.6-22
